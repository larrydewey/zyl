; Compile-fail: deriving Show for a Secret type is forbidden too
; (E_IMPL_FORBIDDEN).

(deftype _cf_Key (CfKeyW Int))
(impl Secret _cf_Key (defn wipe (self) 0))
(derive _cf_Key Show)

(defn main () 0)
