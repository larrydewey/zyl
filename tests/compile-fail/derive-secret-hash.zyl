; Compile-fail: a record with a Secret field cannot derive Hash, Eq or
; Ord -- that would not be constant-time (E_TRAIT_NOT_DERIVABLE).

(defstruct _cf_L (user String) (pw Secret))
(derive _cf_L Hash)

(defn main () 0)
