; Compile-fail: a slice of a Stack bytebuf sent to another actor would
; outlive the frame that owns its bytes.
; expect-error: E_REGION_ESCAPE

(defn leak (target)
  (let b (bytebuf Stack 16)
    (send target (byteslice b 0 4))))

(defn main () (begin (leak (actor-self)) 0))
