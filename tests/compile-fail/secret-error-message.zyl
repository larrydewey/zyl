; Compile-fail: a Secret in an `error` message is printed by the panic
; (E_SECRET_DEBUG).

(defn _cf_check ((k Secret)) (error (str-concat "bad key " (ffi-call "zyl_int_text" k 1000))))

(defn main () (_cf_check 1))
