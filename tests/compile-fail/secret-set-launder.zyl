; Compile-fail: set! of a secret into a let-mut makes that variable secret;
; branching on it afterwards is E_CT_VIOLATION.

(defn _cf_f ((k Secret))
  (let-mut x 0
    (begin
      (set! x k)
      (if (> x 1) 1 0))))

(defn main () 0)
