; A lowercase field type is not a type parameter; it would leave the field unchecked.
; expect-error: E_UNKNOWN_TYPE
(deftype Box (Bx a))
(defn main () (begin (print (+ 1 (match (Bx "s") (Bx v v)))) 0))
