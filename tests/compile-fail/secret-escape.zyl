; Compile-fail: writing a Secret to a file descriptor takes key material
; straight out of the process. Must be rejected with E_SECRET_ESCAPE
; (compiler/secret_check.zyl).

(use core/core)

(defn _cf_exfiltrate ((key Secret))
  (file-write 2 key))

(defn main () (_cf_exfiltrate 5))
