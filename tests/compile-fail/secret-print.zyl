; Compile-fail: a Secret reaching `print` is key material on a debug
; sink. Must be rejected with E_SECRET_DEBUG (compiler/secret_check.zyl).

(use core/core)

(defn _cf_dump ((key Secret))
  (print key))

(defn main () (_cf_dump 1234))
