; Compile-fail: a Show impl whose text is derived from a Secret field
; leaks it through print (E_SECRET_DEBUG).

(defstruct _cf_Login (user String) (pw Secret))
(impl Show _cf_Login (defn show (self) (str-concat self.user (Show.show self.pw))))

(defn main () 0)
