; Compile-fail: the prelude's (impl-not Show Secret) forbids a Show of a
; Secret type's own; its only Show prints `<secret>` (E_IMPL_FORBIDDEN).

(deftype _cf_Key (CfKeyW Int))
(impl Secret _cf_Key (defn wipe (self) 0))
(impl Show _cf_Key (defn show (self) "leak"))

(defn main () 0)
