; Manual Test: read-line (needs stdin input)
; Run: build/boot/zyl-self tests/manual/read-line.zyl -o t.bin && echo 'hello world' | ./t.bin
; Expected: got: hello world

(defn main ()
  (let input (read-line)
    (begin (print (str-concat "got: " input)) 0)))
